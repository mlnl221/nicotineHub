# Proposals — future backlog, no live code

- Worker service plan: **implemented** in `5c65ea9` — deleted. Record is `docs/architecture.md` (`## Worker`) + `docs/spectrum.md`.
- `r_soulseek_improvement_plan.md`: unbuilt backlog (share safety, ProveIt, wildcard bans, webhook, HoneyPot). Worker phases already landed; the rest has no code.
