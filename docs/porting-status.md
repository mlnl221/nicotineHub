# Porting status — vs nicotine-plus 3.3.x / doc/SLSKPROTOCOL.md

> Stage `5c65ea9` — 1:1 parity + worker (scrape/spectrum/tag), mobile-friendly. Desktop-only bits intentionally omitted. Settings port Phases A–N done; `lastfm/librefm/listenbrainz` scrobblers and global font pickers omitted per user request.

| Domain | Ported (done) | Partial / stub | Missing / intentional omit |
|---|---|---|---|
| **Network / Distributed** | Login `1` + `SetWaitPort 2` + `SharedFoldersFiles 35`, `ConnectToPeer 18` direct+relay race 45s, `MAX_SOCKETS` dynamic `min(2/3·ulimit,2048)` (`env MAX_SOCKETS` wins), `ServerPing` fallback, `ReLogged 41`, **leaf→branch** `HaveNoParent 71` 4-msg bootstrap, `PossibleParents 102` 10 dials, `_adoptParent` on `DistribSearch 3`, `BranchLevel 126/Distrib 4` +1 + server notify, `BranchRoot 127/5`, `EmbeddedMessage 93` server-parent, `AcceptChildren 100` toggle via `uploadSpeed`/`ParentMinSpeed 83`/`Ratio 84` child limit `min(speed//ratio//100,10)`, **PortMapper UPnP-only** (`portmapper.ts` lease 43200/renew 7200, `LISTEN_PORT` 60754 editable), `interface` stored (browser text + bridge `env INTERFACE`), `autoreply` + `autoaway 15 → SetStatus 28` timer 60s + `autojoin` (50 rooms) / `userlist` (100 watch) / `autosearch` (20 terms) on login (`session.ts:419`) | `RelatedSearch 153` (low-value spell-correct, not implemented), `ChildDepth 7/129` obsolete (parsed, unused) |  |
| **Search** | Global `FileSearch 26`, `UserSearch 42`, `RoomSearch 120` (scoped validation vs joined/public + autocomplete), `WishlistSearch 103` manual + `WishlistInterval 104` auto 12m/2m timer (`session.ts:514`) + `onWishlistEvent` → `search:result` auto-creating `wishlist:<term>` tab (`search.tsx:95`), `FileSearchResponse 9` zlib 16M/128M + >2GiB sentinel, `ExcludedSearchPhrases 160`; filters `include/exclude/size/bitrate/length/type/slot/country` via `filter.ts` + `filter.worker.ts` (`matchCountry` via `getCountryCode` bisect, eager `GetPeerAddress` batch after `search:result`), grouped `user_grouping`/`folder_grouping` with `partial` expand (`SearchesSection.tsx:78`), `SearchFilterHelp` popover (`preferences.py:2903`) in `SearchBar.tsx:72` |  | Room `user_grouping` `@dnd-kit` drag (replaced by `↑/↓` order controls, intentional), `filtercc` history 50 persists but not shown as UI history dropdown |
| **Transfers — Downloads** | `QueueUpload 43` + `TransferRequest 40 dir1` + `PlaceInQueueRequest 51` 300s poll + `PlaceInQueueResponse 44`, resume `INCOMPLETE<md5>+basename` `ab+`, `FileOffset 8` (`0xFFFFFFFFFFFFFFFF` clamp for >2GiB NS bug, `transfers.ts:991`) + throttle `DOWNLOAD_LIMIT` adaptive, `Finished → GET /files/:token`, `Filtered` via `downloadfilters`, `autoclear`/`usernamesubfolders`, `groupdownloads` grouping (folder/user) in `downloads/page.tsx:61`, legacy `TransferRequest 0` → `handleQueueUpload` for slskd interop, `geoblock` re-check after `GetPeerAddress` via `handlePeerAddressResolved` (`server.ts:591`) |  | `afterfinish/afterfolder` shell (desktop command, intentional omit) |
| **Transfers — Uploads** | `UploadDenied 50`/`Failed 46` + `Queued` with `banlist/ipblocklist/geoblock` `networkfilter.ts`, `filelimit 100`/`queuelimit 10000` + `friendsnolimits`, `FIFO` vs `RoundRobin` + `preferfriends` privileged/buddy, `SendUploadSpeed 121`, `shouldBlockUser` bisect `getCountryCode` with `peerIp=""` defer to `peer-address` re-check, `reveal_buddy_shares`/`reveal_trusted_shares` gating via `ShareDB.getFoldersForPermission` (`shares.ts:384`) + `PortMapper` | `Pending shutdown` graceful (container SIGTERM → `Pending shutdown` status, 5s drain, intentional light) |  |
| **Shares** | `ShareDB` split `public/buddy/trusted` buckets `PermissionLevel` (`shares.py:77` parity, `shares.ts:24` enum) + `share_filters` (`.*`, `Thumbs.db`, `#recycle\`), `scanFsShares` SLSK-only (`attrs []` — bridge never parses; worker `POST /analyze` owns TinyTag parity if needed), `buildSharedFileListResponse 5` + `FolderContents 36/37` zlib lvl4 + `PermissionLevel` gating (leak fixed, `session.ts:1955` `getSharePermissionLevel` → `PUBLIC/BUDDY/TRUSTED/BANNED`), `shouldThrottle` 400ms, `virtual2real`/`real2virtual` backslash sentinel (`shares.ts:212`), `check_shares_available` warning banner in `BrowseView.tsx:139` + `DiagnosticsPage`, `Download Folder` batch (`BrowseView.tsx:272`) |  |  |
| **Browse** | `requestSharedFileList` + `requestFolderContents` with `allowedPeerResponses` 448M guard + 30s timeout, `BrowseTabs` 10 LRU + `nicotineHub.recentBrowse`, Sortable `name/size/bitrate/length` persisted `nicotineHub.browse.sort` + `Download Folder`, `expand_folders` (`userbrowse.expand_folders` `all/partial/none` via `BrowseView.tsx:33`, `all` auto-selects first folder, `none` stays collapsed) | column widths `config.columns` not persisted (flex list on mobile, intentional omit), multi-folder select (single `selectedFolder` only; bulk via `Download Folder` per folder) | `slsk://user/path` copy works, `navigator.registerProtocolHandler` not attempted (browser limitation, `urls.protocols` stub) |
| **Chat Rooms** | `SayChatroom 13` + `JoinRoom 14`/`Leave 15`/`UserJoined 16`/`Left 17` + `RoomList 64`, private rooms `AddRoomMember 134`/`Remove 135`/`CancelMembership 136`/`Ownership 137`/`Granted 139`/`Revoked 140`/`AddOperator 143`/`Remove 144`/`Granted 145`/`Revoked 146`/`Operators 148`, `RoomTickers 113`/`Added 114`/`Removed 115`/`SetRoomTicker 116`, global `150-152`, `JoinRoom` sanitizes `[^ -~]/\s+/24`, `/me` `* `, censor `words.censorwords` word-boundary `chatFormat.ts`, `replaceWords` on send, `readroomlines 200` `truncateMessages` + `rooms_timestamp %X` via `formatStrftime`, `isIgnored` dim, `RoomTicker` global wall (all rooms `allTickers` modal, `chat/page.tsx:421` `roomwall.py` parity) + per-room ticker strip + `SetTicker` input, `EnableRoomInvitations 141` → `chatrooms.user_list_visible` toggle (`ChatsSection.tsx:18`, `chat/page.tsx:385` `aside` conditional), `Completion` `words.tab/dropdown/characters/roomnames/buddies/roomusers/commands` via `useCompletion.ts` `Tab` cycle + dropdown (`chatrooms.py:690` parity) |  | `Completion` `@dnd-kit` drag replaced by `↑/↓` (intentional), `RoomTicker` wall search filter stub |
| **Private Chat** | `MessageUser 22` + `Ack 23` + `MessageUsers 149` batch, `censorText`/`replaceText` + `readprivatelines 200` (`truncateMessages` + `private_timestamp` via `formatStrftime`), `ctcp.enable` `VERSION` gate with 1s throttle (`privateChat.tsx:43` `ctcpThrottle`), `autoreply` away `server.autoreply` + `autoaway 15 → SetStatus 28` (`session.ts:419`), offline `GetPeerAddress` queue via `pendingPrivate` `localStorage` flush on reconnect, Typing `TYPING` `0x01` CTCP debounce 500ms + indicator 3s (`privateChat.tsx:272`), `MessageAcked` persist `privatechat.users` order (move acked user to top + `localStorage nicotineHub.privatechats`) |  | Typing via CTCP (Soulseek has no native typing, nicotine shows via CTCP, parity) |
| **Buddies** | `WatchUser 5`/`Unwatch 6` + `GetUserStatus 7`/`Stats 36` polling, `buddies` 100 LRU `localStorage nicotineHub.buddies`, `trusted/notify` toggles, grid cards `avgspeed/files`, `buddylistinchatrooms tab/chatrooms/always` stored (`UiSection.tsx:99`, currently `/buddies` page + chat `aside` embeds when `chatrooms`), `country flag_XX` via `getCountryCode` + regional flag emoji (`buddies/page.tsx:157` `String.fromCodePoint`), per-buddy `note` + `buddy_notes` persist + `lastSeen` `Never seen` |  |  |
| **Interests** | Likes `AddThingILike 51`/`Remove 52`, hates `117/118`, `Recommendations 54`/`Global 56`/`SimilarUsers 110`/`ItemRecommendations 111`/`ItemSimilarUsers 112`, chips + `For "item"` drill-down, Global vs personal split header (`likes.length===0 ? Global : Based on likes` `interests/page.tsx:296`), expiry 12m `setInterval` auto-refresh (`interests.tsx:33`), Wishlist tie-in `Add to Wishlist` in `interestsMenu` (`menus.ts:274`, `interests/page.tsx:388`) |  | Wishlist tab tie-in via `search:wishlist` already covered in Search |
| **Profiles** | `UserInfoRequest 15`/`Response` `descr/pic/totalupl/queuesize/slotsavail/uploadallowed` (`profileView` `StatCard`), hide/show `pic` `nicotineHub.showPictures` + `picture_visible`, `givePrivileges 123`/`checkPrivileges 92`, `ClipboardItem` copy, `whois` `GetPeerAddress 3` `slotsFull` grey `opacity-60` on avatar + stats (`ProfileView.tsx:241,373`) + tooltip, `UserInterests 57` shown in profile (`ProfileView.tsx:391`) + self interests fetched on `interests.tsx` mount, `SimilarUsers` shortcut button to `/interests` (`ProfileView.tsx:419`) |  |  |
| **Notifications / Window** | `notifications` 11 toggles `notification_window_title/tab_colors/popup_*_wish` `defaults.ts:216`, `Notification.permission` + Service Worker `public/sw.js` `showNotification` + `NotificationsSection` enable button, `window.title` flash, `notification_popup_sound` `Audio` base64 beep |  | Tray `trayicon/startup_hidden/minimize` desktop, `VAPID` push signing (LAN, intentional omit) |
| **Logging / Diagnostics** | `DiagnosticsPage` 355→~420 lines live tail 500/2000 ring `diagnostics.log` + level/scope/search, `PortChecker` + `StatisticsPanel`, `LoggingSection` `privatechat/chatrooms/transfers/debug`, `log_timestamp` format via `formatStrftime` on Diagnostics (`diagnostics/page.tsx:14` `settings.logging.log_timestamp`), `readroomlines` `truncateMessages` on new msg + initial load (`rooms.tsx:87`), `logcollapsed` grouping by `scope` (`details` per scope) |  | Per-day `roomlogsdir/privatelogsdir/transferslogsdir` rotation `logfacility.py` (single `diagnostics.log` ring 500/2000, intentional for browser; `roomlogsdir` paths stored as text notes) |
| **Statistics** | `StatsManager` `since_timestamp/started/completed_downloads/uploaded` `statistics.json`, `StatisticsPanel` total vs session + `Reset` → `statistics:reset` → `TransferManager.resetStats()`, `since` humanized `fmtSince` (`StatisticsPanel.tsx:13` `X days/hours ago`) |  | Per-session `downloads_YYYY-MM-DD.log` (single `statistics.json`, intentional omit) |
| **Plugins** | `PluginManager` `plugins.json` + builtins `spamfilter` + `leech_detector` + `core_commands` (`/help` + `/plugin` only) `returncode.zap/break/pass`, `install*` (zip/URL/GitHub-TS) disabled — builtins only, `PLUGININFO` `settings/metasettings` grouped by `meta.group` into per-group cards (`PluginsSection.tsx:254`); `leech_detector` ported — `num_files/num_folders` thresholds + `detected_leechers` + `upload_queued/stats/finished` hooks (`plugins/builtin/leech_detector.ts`) |  | `youtube_info` **intentionally not ported** — requires YouTube Data v3 external API key + HTTP, not homelab-relevant |
| **Settings** | 17 tabs (`Network`, `Appearance`, `Shares`, `Downloads`, `Uploads`, `Searches`, `User Profile`, `Chats`, `Now Playing`, `Logging`, `Banned`, `Ignored`, `URL Handlers`, `Plugins`, `Worker`, `Notifications`, `About`) grouped nav + `?tab=`/`#tab` deep-link; all `defaults.ts` 15 sections wired (`SharesSection` folder picker; `Downloads/Uploads` full `transfers` knobs; `UserProfile` WebP publish; `Chats` censor/replace/mentions; `NowPlaying` tokens; `Logging`/`Banned`/`Ignored`; URLHandlers `protocols` stub; `Plugins` toggle + `Worker` tokens/health) — `Network: interface/autoreply/autosearch/autojoin/userlist` done (Phase H) + `UI: dark_mode/file_size_unit exact vs humanized/reverse_file_paths swap/usernamehotspots/style/spellcheck/tabclosers/tab_select_previous/buddylistinchatrooms (chatrooms/always → buddies panel in Chat)/header_bar/exitdialog/filters_visible` + `Window` `width/height/maximized` via `WindowGeometrySync.tsx` (PWA viewport, `xposition/yposition` -1) — **color/font pickers omitted intentionally** (`DESIGN.md`); **English-only** (30+ `po/` locales not ported) |  | `portrange/upnp` via bridge `PortMapper` (done), tray `trayicon/startup_hidden`/`xposition`/`yposition`, `filemanager` `desktop.ini`, `speech` deprecated `3.4.0` |
| **Now Playing** | `npplayer/npformat/npothercommand/npformatlist` `NowPlayingSection.tsx`, `NowPlayingSync` `mediaSession` (browser `mediaSession` polling 5s only), token legend `$n $t $a $b` + `Test` preview (`formatNowPlaying` sample) + history `npformatlist`; `mpris/other` stored-only | `npothercommand` shell not executed in browser (stored only) | `lastfm/librefm/listenbrainz` **intentionally omitted** — desktop/cloud scrobbler (`ws.audioscrobbler.com`) has no web equivalent; `npformat`/`mediaSession` covers browser Now Playing |
| **PWA / Mobile shell** | `TopBar` 60 + `env(safe-area)`, `BottomNav` 64 + safe-area, `Sidebar` `w-72`, `ThemeProvider` dark, `manifest.webmanifest`, `ThroughputChart` 2s, `modes_visible/modes_order` drag via `↑/↓` order controls `UiSection.tsx` + `Sidebar/BottomNav` filtered (full `@dnd-kit` drag omitted) |  | `Global font` pickers **intentionally omitted** per user request + `docs/DESIGN.md` — Alexandria `Noto Serif/Inter/Public Sans` fixed; `icon_view` preview omitted |

*Intentionally omitted (no web equivalent, see `docs/DESIGN.md`):* `server.passw` plaintext, `interface` raw bind (stored only, `env INTERFACE` on bridge), tray `trayicon`/`startup_hidden`/`xposition`/`yposition` (desktop position, PWA centered -1), `ui.filemanager` `xdg-open`, `urls.protocols` execution (stored as lines), OS `MPRIS`/`speech`, desktop `afterfinish/afterfolder` shell, **Now Playing `lastfm/librefm/listenbrainz`** scrobblers (`ws.audioscrobbler.com` polling — no browser API; `npformat`+`mediaSession` kept), **global font pickers** `globalfont/.../browserfont` (Alexandria editorial palette fixed per `docs/DESIGN.md` + user request), **`youtube_info` plugin** (YouTube Data v3 API key + external `www.googleapis.com` — intentionally not ported), **i18n** — English-only by design (30+ `po/` locales not ported), **window `xposition/yposition` -1** (PWA centered). `portrange`/`upnp` and `width/height/maximized` are **not** omitted — `LISTEN_PORT` 60754 editable in Settings → Network and `width/height/maximized` via `WindowGeometrySync.tsx`.
