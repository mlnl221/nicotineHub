# deps
node_modules/
**/node_modules/

# build output
**/dist/
**/.next/
**/.turbo/
**/dist/**/node_modules/

# logs / env
*.log
.env
.env.local

# repo files not needed inside the app images
docs/
compose.yaml
*.md
!ATTRIBUTION.md

# e2e / playwright — host only, never needed in Docker (was slowing builds ~10-15s + 50MB)
e2e/
playwright.config.ts
test-results/
playwright-report/
.github/

# vcs / os
.git/
.gitignore
.DS_Store
