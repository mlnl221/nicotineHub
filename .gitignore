# deps
node_modules/

# build output
dist/
.next/
out/
apps/web/.next/

# env (per-worktree overrides — see AGENTS.md Git Worktrees)
.env
.env.local
*.local
compose.override.yaml

# local data dirs (bridge CONFIG_DIR/DATA_DIR fallback: ./config, ./data, /tmp; holds
# worker.json with secret tokens when saved via Settings → Worker)
/config/
/data/
apps/bridge/data/
apps/bridge/config/

# bun / npm
bun.lockb

# logs
*.log
npm-debug.log*

# geoip data — fetched at docker build, not committed (3.9M)
apps/bridge/src/data/ip_country_data.csv
apps/bridge/dist/data/ip_country_data.csv

# os / editor
.DS_Store
.idea/
.vscode/
*.tsbuildinfo

# demo safety — large FLACs must never be committed (demo ships small Vorbis .ogg only)
*.flac
# python (worker)
__pycache__/
*.py[cod]
.pytest_cache/
.venv/

.playwright-mcp/
test-results/